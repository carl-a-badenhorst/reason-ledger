import { PageStub } from "@/components/page-stub";

export default function ChangesPage() {
  return (
    <PageStub
      title="Change log"
      description="Stub for sync, export/import, and object change history. Refined in Phase 9."
    />
  );
}
