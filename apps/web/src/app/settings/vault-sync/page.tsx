import { PageStub } from "@/components/page-stub";

export default function VaultSyncSettingsPage() {
  return (
    <PageStub
      title="Vault sync"
      description="Configure REASON_LEDGER_VAULT_PATH, run export/import, and inspect SyncRecord state. Phases 7–8."
    />
  );
}
