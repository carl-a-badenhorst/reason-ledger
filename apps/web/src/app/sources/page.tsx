import { PageStub } from "@/components/page-stub";

export default function SourcesPage() {
  return (
    <PageStub
      title="Sources"
      description="Imported sources and explorer. Phase 2 adds import, storage, and listing."
    />
  );
}
