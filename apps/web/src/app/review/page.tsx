import { PageStub } from "@/components/page-stub";

export default function ReviewPage() {
  return (
    <PageStub
      title="Review"
      description="Human and system review activity across objects. Populated from ReviewEvent in later phases."
    />
  );
}
