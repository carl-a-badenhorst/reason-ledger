import { PageStub } from "@/components/page-stub";

export default function ClaimsPage() {
  return (
    <PageStub
      title="Claims inbox"
      description="Claims filtered by status and review queue. Phase 4 implements the claim model and inbox UX."
    />
  );
}
